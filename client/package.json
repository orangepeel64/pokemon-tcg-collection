{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "wtc-perspective-card": "^1.0.2"
  },
  "scripts": {
    "start": "parcel --port 3000 index.html",
    "build": "parcel build index.html --dist-dir ../server/public"
  },
  "devDependencies": {
    "parcel": "^2.12.0"
  }
}